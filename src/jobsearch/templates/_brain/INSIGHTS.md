# INSIGHTS — append-only learning log

The AI writes here after a completed task. You can edit or delete entries,
but the AI never will. Each entry: a dated header, a paragraph, a tag.

Tags:
- `pattern` — something that worked, reuse it
- `gap` — data missing in the system that hurt the application
- `rule` — a new constraint the user voiced

The AI reads the **last 20 entries** at the start of every session.

---

## 2026-01-01 — pattern

This is a seed entry so the file is not empty. Replace it once you start
tracking real postulations. Example:

> After the IRC Senior MEAL application, the recruiter responded fastest to
> the version of the letter that opened with "8 countries, 4 donors" instead
> of the tool stack. For NGO roles, lead with geographic and funder breadth.
